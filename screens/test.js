state = {find_workouts: false, maintain_weight: false,birth_preparation: false,feel_relaxed: false}

console.log(state)

console.log(state['find_workouts'])
//state.map(todo =>
//            todo.id === action.id ? { ...todo, completed: !todo.completed } : todo
//        )

